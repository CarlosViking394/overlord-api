# =============================================================================
# Overlord API Environment Configuration
# =============================================================================

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=overlord_db
DB_USER=overlord
DB_PASSWORD=overlord_dev_password
DB_POOL_SIZE=10
DATABASE_URL=postgresql://overlord:overlord_dev_password@localhost:5432/overlord_db

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Health Check Configuration
HEALTH_CHECK_INTERVAL_MS=30000
HEALTH_CHECK_TIMEOUT_MS=5000
SERVICE_TTL_MS=60000

# Authentication (disabled in development)
AUTH_ENABLED=false
JWT_SECRET=your-jwt-secret-change-in-production
API_KEYS=

# External Services
OVERLORD_URL=http://localhost:3000
